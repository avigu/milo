
## Memory Flush - 2026-04-26

**Key Events & Learnings from this Session:**

*   **Bootstrap & Identity:** Successfully completed initial bootstrap. Established identity as Milo (🐾), a helpful and creative AI assistant. User is Avi.
*   **WhatsApp Setup:** Initial setup failed due to `allowFrom` policy and listener issues. Successfully resolved by manually updating `openclaw.json` (multiple times due to config resets on container restarts) and re-linking the channel.
*   **Daily Riddle Web App:**
    *   Built a simple Flask web app (`app.py`, `riddles.json`, `templates/index.html`).
    *   Deployed it on Hostinger VPS.
    *   Initially ran on port 5000, then 80, then 8080, then 3000, and finally back to 8080.
*   **Network & Deployment Troubleshooting:**
    *   Encountered `404` errors due to a default web server (later identified as Traefik) on port 80.
    *   Discovered Hostinger's external firewall was blocking non-standard ports (5000, 8080, 3000).
    *   Learned about Avi's `Traefik` container running in `host` network mode.
    *   Successfully configured Traefik routing by adding labels to `openclaw` service in `docker-compose.yml` (using `HostRegexp`).
    *   Crucially, diagnosed and fixed the missing `ports: - "8080:8080"` mapping in `docker-compose.yml`, which was preventing external access to the app, despite Traefik routing.
*   **ACP Sub-agent (Claude Code):**
    *   Attempted to spawn an ACP sub-agent for Claude Code using `runtime: "acp"`.
    *   Encountered "thread paradox" (webchat doesn't support threads for persistent ACP sessions) and "ACP runtime backend is not configured" errors.
    *   Determined that `acpx` plugin needs proper installation, not just enablement in `openclaw.json`.
*   **Invest Coach Project:**
    *   Received `FinApp.zip` archive and extracted it.
    *   Reviewed `README.md` and technical specifications for the "Invest Coach" app.
    *   Generated `TECHNICAL_DESIGN.md` and `IMPLEMENTATION_PLAN.md` based on project brief.
    *   Started Next.js project (`invest-coach-web`) and implemented "Direction D" UI with mock data (Phase 1).
    *   Successfully deployed Invest Coach dashboard to port 8080 (via Traefik) after riddle app was moved.
*   **VS Code Server (`code-server`):**
    *   Installed `code-server` on port 8081 for web-based IDE access.
    *   Installation was wiped upon Docker container restarts.
    *   Successfully reinstalled `code-server` and restarted it on port 8081.
    *   Current Status: VS Code Server is running internally, but port 8081 is still blocked by Hostinger's external firewall from Avi's perspective.
*   **General Learnings:** Docker Compose restarts can wipe non-persistent container changes. Hostinger's network and firewall policies are complex and require explicit port opening and Traefik configuration. The `PORT` env var in the container was causing conflicts.
